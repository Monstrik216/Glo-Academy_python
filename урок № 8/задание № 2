print('введите число:')
a = int(input())
total = 0
while a != 0:
    last_digit = a % 10
    if last_digit == 5:
        total += 1
    a //= 10
print('количество цифр 5 среди всех цифр введеного числа:',total)

print('введите число:')
a = int(input())
total = 0
while a != 0:
    last_digit = a % 10
    if last_digit == 5:
        total += 1
    a //= 10
print('количество цифр 5 среди всех цифр введеного числа:', total)
